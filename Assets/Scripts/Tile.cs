using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Tile : MonoBehaviour
{
    public GameObject logGeo;

    // Start is called before the first frame update
    void Start()
    {
        //Instantiate(logGeo, new Vector3(-4, 0, 1), Quaternion.identity);
        //Instantiate(logGeo, new Vector3(4, 0, 1), Quaternion.identity);
        //Instantiate(logGeo, new Vector3(0, 0, -2), Quaternion.identity);

    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
